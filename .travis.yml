language: go
go:
- 1.8.x

os:
  - osx
  
sudo: false

install:
- go get ./...

script:
- make test

after_success:
  - [[ test -n "$TRAVIS_TAG" && curl -sL https://git.io/goreleaser | bash

notifications:
  email: false
