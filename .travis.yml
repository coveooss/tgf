language: go
go:
- 1.8.x

sudo: false

install:
- go get ./...

script:
- make test

after_success:
  - [[ $TRAVIS_TAG == v[0-9]* ]] && curl -sL https://git.io/goreleaser | bash

notifications:
  email: false
